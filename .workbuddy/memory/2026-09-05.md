# 2026-09-05

## 烬营远征：配置 Capacitor 安卓打包（未生成 APK）
- 安装 @capacitor/core、@capacitor/android、@capacitor/cli 均为 **8.5.1**。
- 新增 `capacitor.config.ts`：appId `com.embercamp.expedition`，appName `烬营远征`，webDir `dist`，androidScheme https。
- index.html 加移动端 viewport（user-scalable=no、viewport-fit=cover、theme-color、body 黑底防滚动）。
- android/ 工程为**手工修复的半成品**（此前某次初始化残留，且 Android Studio 开着锁住目录无法删除重建）：
  - 修正 `app/build.gradle` namespace/applicationId → com.embercamp.expedition
  - 修正 `strings.xml` 应用名与包名
  - 重写 `com/embercamp/expedition/MainActivity.java` 的 package 声明
  - 手工完成 cap sync 的工作（本环境 safe-delete 组件坏了，所有删除操作 fail-closed，导致 `cap sync`/`cap add` 内部删除步骤失败）：
    - 从 `node_modules/@capacitor/cli/assets/*.tar.gz` 模板解出 capacitor-cordova-android-plugins 模块
    - 手写 `capacitor.settings.gradle`、`app/capacitor.build.gradle`、插件模块 `capacitor.build.gradle`（无第三方插件的最小内容）
    - `cp -r dist/. android/app/src/main/assets/public/`（assets 里已有正确的 capacitor.config.json / capacitor.plugins.json / cordova.js）
  - `npx cap doctor android` 通过。
- package.json 增加 `cap:sync` / `cap:copy` / `cap:open` 脚本；.gitignore 补 android 构建产物。
- **待手动清理**（环境删除功能失效删不掉，对构建无影响）：
  - `.cap-tmp/`（模板解压临时目录）
  - `android/app/src/main/java/com/getcapacitor/`（模板孤儿 MainActivity，未被引用）
  - `android/capacitor-cordova-android-plugins/plugins/`（误嵌套的重复目录）
- 后续打包：Android Studio 打开 android/ → Build；或命令行 `cd android && gradlew assembleDebug|assembleRelease`（需 JDK 21）。
